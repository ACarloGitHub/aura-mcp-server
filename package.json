{
  "name": "lm-studio-agent-server",
  "version": "1.0.0",
  "description": "MCP server for LM Studio with agent personality, wiki, planner, session compaction, and cross-platform support",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepare": "npm run build"
  },
  "keywords": ["mcp", "lm-studio", "agent", "wiki", "planner", "personality"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
