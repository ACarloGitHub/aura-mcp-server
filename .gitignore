node_modules/
dist/
*.log
.DS_Store
.env
wiki/raw/*/
rag/chroma_data/
session-exports/
compacted-sessions/

# GGUF model files (download via instructions in README)
models/*.gguf
!models/.gitkeep

# Python virtual environment
venv/

# Python cache
__pycache__/
*.pyc
# Syncthing conflict files
*.sync-conflict*

# API keys and personal config (create from *.example.json templates)
api-key.json
lm-studio-config.json

# Backup files
SOUL.bak
*.bak

# Backup folder
backup/

# Log file
mcp-server.log
*.log

# Local scratch (mock-client baseline capture, smoke-test artifacts)
tmp/

# Documentation (working materials, not part of the public repo)
docs/

# Logs (created at runtime by the launcher if needed)
logs/

# Note: vendor/llama.cpp/ and embeddings/ are INTENTIONALLY committed
# so the GitHub Actions release workflow can bundle them into the OS-native
# installer without re-downloading from third-party hosts.

# Tauri build outputs
src-tauri/target/
src-tauri/Cargo.lock
