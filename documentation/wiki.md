# Wiki (v3.1)

The wiki is a Karpathy-style markdown knowledge base under `Wiki/` inside the workspace. `wiki` action values handle read/write/search/list; `wiki_ingest` handles the structured knowledge-graph side.

## Layout

```
Wiki/
├── summaries/      # One-page summary per source file
├── concepts/       # Reusable concepts (cross-file)
├── entities/       # People, places, things mentioned
├── syntheses/      # Cross-concept syntheses
├── projects/       # Project-specific pages
├── wiki/
│   ├── index.md    # Auto-generated by wiki_ingest(action=update_index)
│   └── log.md      # Append-only log (wiki_ingest(action=update_log))
└── raw/            # Original raw files (input to wiki_ingest)
```

## `wiki` actions

| Action | Args | Returns |
|---|---|---|
| `search` | `query`, optional `maxResults` | List of pages where body matches `query` (case-insensitive). Snippet per page capped at 300 chars (`LIMITS.wikiSnippet`). |
| `read` | `path` | Page body capped at 4,000 chars (`LIMITS.wikiBody`). Plus modification date in the footer. |
| `write` | `path`, `content` | Saves the page (creates parents). |
| `list` | optional `maxResults` | List of `{path, title, modified}`. `action=list` populates `structuredContent` with `{ total, shown, pages }`. |

Each multi-line result starts with `[INSTRUCTION: ...]` (phase 2 of v3.0). The instruction tells the model how to summarize — for `read` it's "reference or quote from it as needed; do NOT restate the whole page back to the user".

## Permissions

`wiki(read)`/`wiki(write)` validates the requested page path is inside `Wiki/` and against the permission store. Path traversal returns a `Permission: ...` error with `pendingApproval: true` before the call hits disk, instructing the agent to ask the user for permission.

## `wiki_ingest` actions

The advanced / structured side. Companion to `wiki` for organising the body of knowledge.

| Action | Args | Purpose |
|---|---|---|
| `ingest` | `source` (path under `Wiki/raw/`) | Loads a raw file and emits the role-reminder instructions for the model to write summaries, concepts, entities and links. |
| `ingest_wiki` | — | **Fixes the curated wiki into the RAG.** Scans `Wiki/` for `.md` pages (excluding `raw/`, `test/`, `ritest/`, `index.md`, `log.md`) and upserts each into the RAG collection `wiki` (id = page path, metadata `{type, title, source}`). Re-running updates in place (upsert by id), so the RAG always mirrors the current wiki. Does **not** touch `wiki/index.md`. |
| `query` | `query_text` | Surfaces the index, prompting the model to drill down via `wiki(search|read)`. |
| `lint` | — | Integrity check: missing frontmatter, orphan pages, missing `confidence:`. |
| `update_index` | — | Rebuilds `wiki/index.md` from the on-disk content. |
| `update_log` | `source` (operation description) | Appends a dated entry to `wiki/log.md`. |

## Workflow

1. Drop a source file in `Wiki/raw/`.
2. Call `wiki_ingest(action=ingest, source="...")`. The model receives the file content with instructions to write `summaries/`, `concepts/`, `entities/` pages and update the index.
3. After the model writes pages, call `wiki_ingest(action=update_index)` and `wiki_ingest(action=update_log, source="ingested <name>")`.
4. Run `wiki_ingest(action=ingest_wiki)` to fix the curated pages into the RAG `wiki` collection, then use `rag(action=search, collection="wiki")` for semantic recall.
5. Periodically run `wiki_ingest(action=lint)` to surface missing frontmatter or orphan pages.
6. Use `wiki(search|read)` for fast keyword lookup of the curated knowledge.

## See also

- [compaction.md](compaction.md) for memory management patterns.
- [planner.md](planner.md) for using the wiki during multi-step planning.
- [architecture.md](architecture.md) for how the wiki fits in the system.
