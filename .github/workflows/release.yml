name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch: {}

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  build:
    name: Build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            args: ""
          - os: macos-latest
            args: "--target universal-apple-darwin"
          - os: ubuntu-latest
            args: ""

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install Linux dependencies
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            libxml2-utils \
            file \
            wget

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Setup Rust targets (macOS universal)
        if: matrix.os == 'macos-latest'
        run: |
          rustup target add aarch64-apple-darwin
          rustup target add x86_64-apple-darwin

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: "src-tauri -> target"

      - name: Install npm dependencies
        run: npm ci --ignore-scripts

      - name: Force re-embed frontend (invalidate Rust cache for our crate)
        run: cargo clean -p auramcp-server
        working-directory: src-tauri

      - name: Build application
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: "AuraMCP ${{ github.ref_name }}"
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: auramcp-server-${{ matrix.os }}-bundles
          path: |
            src-tauri/target/**/bundle/msi/*.msi
            src-tauri/target/**/bundle/nsis/*-setup.exe
            src-tauri/target/**/bundle/dmg/*.dmg
            src-tauri/target/**/bundle/macos/*.app.tar.gz
            src-tauri/target/**/bundle/deb/*.deb
            src-tauri/target/**/bundle/rpm/*.rpm
            src-tauri/target/**/bundle/appimage/*.AppImage
          if-no-files-found: warn
          retention-days: 7

  release:
    name: Publish release with all artifacts
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List downloaded artifacts
        run: |
          echo "=== Downloaded artifacts ==="
          find artifacts -type f -print
          echo "=== File sizes ==="
          find artifacts -type f -exec ls -lh {} \;

      - name: Generate changelog from Conventional Commits
        id: changelog
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            const workspace = process.env.GITHUB_WORKSPACE || process.cwd();
            console.log(`[changelog] workspace: ${workspace}`);
            console.log(`[changelog] cwd: ${process.cwd()}`);

            try {
              const owner = context.repo.owner;
              const repo = context.repo.repo;
              const currentTag = context.ref.replace(/^refs\/tags\//, '');
              console.log(`[changelog] current tag: ${currentTag}`);

              // 1. Find previous stable tag
              const { data: tags } = await github.rest.repos.listTags({
                owner, repo, per_page: 10,
              });
              const stableTags = tags
                .map(t => t.name)
                .filter(n => /^v\d+\.\d+\.\d+$/.test(n))
                .filter(n => n !== currentTag)
                .sort((a, b) => b.localeCompare(a, undefined, { numeric: true }));
              const previousTag = stableTags[0] || null;
              console.log(`[changelog] previous tag: ${previousTag || '(none)'}`);

              // 2. Get commits
              let commits = [];
              if (previousTag) {
                const { data } = await github.rest.repos.compareCommits({
                  owner, repo, base: previousTag, head: currentTag,
                });
                commits = data.commits;
              } else {
                const { data } = await github.rest.repos.listCommits({
                  owner, repo, per_page: 50,
                });
                commits = data;
              }
              console.log(`[changelog] commits in range: ${commits.length}`);

              // 3. Categorize by Conventional Commits prefix
              const whatsnew = [];
              const improvements = [];
              const breaking = [];
              for (const c of commits) {
                const msg = c.commit.message.split('\n')[0];
                if (/^[a-zA-Z0-9_-]+!:/.test(msg) || /BREAKING CHANGE:/i.test(c.commit.message)) {
                  breaking.push(msg);
                } else if (/^(feat|fix)(\([^)]+\))?!?:/i.test(msg)) {
                  whatsnew.push(msg);
                } else if (/^(refactor|perf|docs)(\([^)]+\))?!?:/i.test(msg)) {
                  improvements.push(msg);
                }
              }
              console.log(`[changelog] whatsnew=${whatsnew.length} improvements=${improvements.length} breaking=${breaking.length}`);

              // 4. Build body
              const fmt = (arr) => arr.length
                ? arr.map(m => '- ' + m).join('\n')
                : '_No changes in this category - see commits on GitHub._';
              const sections = [
                "## What's new",
                fmt(whatsnew),
                "## Improvements",
                fmt(improvements),
              ];
              if (breaking.length > 0) {
                sections.push("## Warning Breaking changes");
                sections.push(fmt(breaking));
              }
              sections.push("## Notes");
              sections.push('- **Data preservation**: your data is preserved across this upgrade.');
              sections.push('- **Uninstall**: removing the app does not delete `%APPDATA%\\com.auramcp.server\\` (Windows), `~/Library/Application Support/auramcp/` (macOS), or `~/.config/auramcp/` (Linux).');
              sections.push('- **No code signing**: AuraMCP is open source (MIT) but not yet code-signed. You may see a SmartScreen warning on Windows or Gatekeeper block on macOS.');
              sections.push("## Downloads");
              sections.push('- **Windows:** `.msi` (WiX) and `.exe` (NSIS) installers');
              sections.push('- **macOS:** Universal `.dmg` (Intel + Apple Silicon), `.app.tar.gz`');
              sections.push('- **Linux:** `.deb`, `.rpm`, and AppImage');

              const body = sections.join('\n\n');

              // 5. Write body to file (absolute path to avoid CWD issues)
              const bodyPath = path.join(workspace, 'release_body.md');
              fs.writeFileSync(bodyPath, body, 'utf8');
              console.log(`[changelog] wrote body to ${bodyPath} (${body.length} chars)`);
              core.setOutput('has_commits', whatsnew.length + improvements.length + breaking.length);
              core.setOutput('body_path', bodyPath);
            } catch (err) {
              console.error(`[changelog] FAILED: ${err.message}`);
              console.error(err.stack);
              core.setFailed(`Generate changelog failed: ${err.message}`);
            }

      - name: Sync release body and tag from changelog file
        id: sync
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            const workspace = process.env.GITHUB_WORKSPACE || process.cwd();
            const bodyPath = path.join(workspace, 'release_body.md');
            const currentTag = context.ref.replace(/^refs\/tags\//, '');

            try {
              if (!fs.existsSync(bodyPath)) {
                console.log(`[sync] no changelog file at ${bodyPath}, skipping`);
                return;
              }
              const body = fs.readFileSync(bodyPath, 'utf8');
              console.log(`[sync] read body (${body.length} chars) from ${bodyPath}`);

              const { data: releases } = await github.rest.repos.listReleases({
                owner: context.repo.owner, repo: context.repo.repo, per_page: 20,
              });
              const target = releases.find(r =>
                r.tag_name === currentTag || r.target_commitish === context.sha
              );

              if (target) {
                console.log(`[sync] deleting pre-existing release id=${target.id} tag=${target.tag_name} (draft=${target.draft}) so we can recreate it with clean URL`);
                await github.rest.repos.deleteRelease({
                  owner: context.repo.owner, repo: context.repo.repo, release_id: target.id,
                });
                console.log(`[sync] deleted release id=${target.id}`);
              } else {
                console.log(`[sync] no existing release for tag ${currentTag} / sha ${context.sha.slice(0,7)}`);
              }

              const { data: created } = await github.rest.repos.createRelease({
                owner: context.repo.owner,
                repo: context.repo.repo,
                tag_name: currentTag,
                name: `AuraMCP ${currentTag}`,
                body: body,
                draft: true,
                prerelease: false,
                target_commitish: context.sha,
              });
              console.log(`[sync] created release id=${created.id} tag_name=${created.tag_name} url=${created.html_url} bodyLength=${created.body.length}`);
              core.setOutput('release_id', String(created.id));
            } catch (err) {
              console.error(`[sync] FAILED: ${err.message}`);
              console.error(err.stack);
              core.setFailed(`Sync release body failed: ${err.message}`);
            }

      - name: Create or update release and attach all artifacts
        uses: actions/github-script@v7
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SYNC_RELEASE_ID: ${{ steps.sync.outputs.release_id }}
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            const releaseId = process.env.SYNC_RELEASE_ID;
            if (!releaseId) {
              core.setFailed('SYNC_RELEASE_ID not set (previous sync step did not run)');
              return;
            }
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            const workspace = process.env.GITHUB_WORKSPACE || process.cwd();
            const artifactsDir = path.join(workspace, 'artifacts');
            const exts = ['.msi', '.exe', '.dmg', '.deb', '.rpm', '.AppImage'];
            const tarGzNames = ['.app.tar.gz'];
            function walk(dir) {
              const out = [];
              for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
                const p = path.join(dir, entry.name);
                if (entry.isDirectory()) out.push(...walk(p));
                else out.push(p);
              }
              return out;
            }
            const allFiles = walk(artifactsDir);
            const files = allFiles.filter(f => {
              const base = path.basename(f);
              if (tarGzNames.some(s => base.endsWith(s))) return true;
              return exts.includes(path.extname(f));
            });
            console.log(`[upload] found ${files.length} files to upload`);
            const mimeMap = {
              '.msi': 'application/x-msi',
              '.exe': 'application/x-msdownload',
              '.dmg': 'application/apple-diskimage',
              '.tar.gz': 'application/gzip',
              '.deb': 'application/x-deb',
              '.rpm': 'application/x-rpm',
              '.AppImage': 'application/x-executable',
            };
            for (const file of files) {
              const base = path.basename(file);
              let contentType = 'application/octet-stream';
              for (const k of Object.keys(mimeMap)) {
                if (base.endsWith(k)) { contentType = mimeMap[k]; break; }
              }
              const data = fs.readFileSync(file);
              const url = `https://uploads.github.com/repos/${owner}/${repo}/releases/${releaseId}/assets?name=${encodeURIComponent(base)}`;
              console.log(`[upload] uploading ${base} (${data.length} bytes)`);
              const res = await fetch(url, {
                method: 'POST',
                headers: {
                  'Authorization': `token ${process.env.GITHUB_TOKEN}`,
                  'Content-Type': contentType,
                  'Accept': 'application/vnd.github+json',
                  'User-Agent': 'AuraMCP-Release',
                },
                body: data,
              });
              if (!res.ok) {
                const txt = await res.text();
                core.setFailed(`upload ${base} failed: ${res.status} ${txt}`);
                return;
              }
              console.log(`[upload] uploaded ${base} status=${res.status}`);
            }
